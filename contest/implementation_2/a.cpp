#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,count=0;
    cin>>n;
    long long int ara[n];
    for(int i=0;i<n;i++)
    {
        cin>>ara[i];
        if(ara[i]%2==0)
        {
            count++;
        }
    }
    if(count=n)
    {
        cnt++;

    }

    return 0;
}
